# ST2110 RDS Manager

NMOS IS-04準拠のRDS (Registration and Discovery System) を管理するWebアプリケーション

## 概要

ST2110環境で使用されるRDSサーバーに接続し、ノードのコピー・削除操作やスケジューリング機能を提供するシステムです。

## 技術スタック

### フロントエンド
- React 18
- TypeScript
- Vite
- Material-UI (MUI)
- Zustand (状態管理)
- React Query (TanStack Query)
- React Hook Form + Zod
- TanStack Table
- react-toastify

### バックエンド
- Node.js 18+
- Express
- TypeScript
- SQLite
- node-cron

## プロジェクト構造

```
st2110-rds-manager/
├── frontend/          # React フロントエンド
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── services/
│   │   ├── store/
│   │   ├── types/
│   │   └── utils/
│   └── package.json
│
└── backend/          # Node.js バックエンド
    ├── src/
    │   ├── controllers/
    │   ├── services/
    │   ├── models/
    │   ├── routes/
    │   ├── scheduler/
    │   └── database/
    └── package.json
```


## クイックスタート

### 🚀 一発起動 (推奨)

**Windowsユーザー:**
```bash
# すべてのサーバーを起動
start-all.bat

# モックRDSサーバー付きで起動
start-all-with-mock.bat

# クイックスタート (最小限のプロンプト)
quick-start.bat

# すべて停止
stop-all.bat
```

**手動起動:**

1. バックエンドとフロントエンドを別々のターミナルで起動:
```bash
# ターミナル1: バックエンド
cd backend
npm install  # 初回のみ
npm run dev

# ターミナル2: フロントエンド
cd frontend
npm install  # 初回のみ
npm run dev
```

2. ブラウザで開く: http://localhost:5173

### バッチファイル説明

| ファイル名 | 説明 |
|-----------|------|
| `start-all.bat` | バックエンド・フロントエンドを起動 |
| `start-all-with-mock.bat` | すべて + モックRDSサーバー2台を起動 |
| `quick-start.bat` | 最小限のプロンプトでクイック起動 |
| `stop-all.bat` | すべてのサーバーを停止 |

## セットアップ

### 必要要件
- Node.js 18以上
- npm または yarn

### インストール

1. リポジトリのクローン
```bash
cd st2110-rds-manager
```

2. バックエンドのセットアップ
```bash
cd backend
npm install
```

3. フロントエンドのセットアップ
```bash
cd frontend
npm install
```

## 開発

### バックエンドの起動
```bash
cd backend
npm run dev
```
サーバーは http://localhost:3000 で起動します

### フロントエンドの起動
```bash
cd frontend
npm run dev
```
開発サーバーは http://localhost:5173 で起動します

## 主要機能

### 実装済み (フェーズ1)
- ✅ プロジェクト基盤構築
- ✅ データベース初期化
- ✅ 基本的なルーティング
- ✅ フロントエンド基本レイアウト

### 実装予定
- Phase 2: RDS接続管理
- Phase 3: ノード操作機能
- Phase 4: スケジューリング機能
- Phase 5: ログ機能

## API エンドポイント

### RDS管理
- `GET /api/rds` - RDS一覧取得
- `POST /api/rds` - RDS登録
- `PUT /api/rds/:id` - RDS更新
- `DELETE /api/rds/:id` - RDS削除
- `POST /api/rds/:id/test` - 接続テスト

### ノード操作
- `GET /api/nodes/:rdsId` - ノード一覧取得
- `GET /api/nodes/:rdsId/:nodeId` - ノード詳細取得
- `POST /api/nodes/copy` - ノードコピー
- `DELETE /api/nodes/:rdsId/:nodeId` - ノード削除

### スケジュール
- `GET /api/schedules` - スケジュール一覧
- `POST /api/schedules` - スケジュール作成
- `PUT /api/schedules/:id` - スケジュール更新
- `DELETE /api/schedules/:id` - スケジュール削除
- `POST /api/schedules/:id/toggle` - 有効/無効切り替え

### ログ
- `GET /api/logs/operations` - 操作ログ
- `GET /api/logs/schedules` - スケジュール履歴
- `GET /api/logs/errors` - エラーログ

## ライセンス

MIT
